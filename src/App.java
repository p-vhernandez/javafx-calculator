import javafx.application.Application;

public class App {

    public static void main(String[] args) throws Exception {
        // Application.launch(Calculator1.class, args);
        // Application.launch(Calculator2.class, args);
        Application.launch(Calculator3.class, args);
    }
}
